###Read Me #######
1. Change the location of the dataset file in project.R 
   "data <- read.csv("C:/Users/pakusruti/Downloads/train.csv")"
   update it with the correct location of training dataset

2. Run the complete program

3. Our program will display the accuracy of all the three classifiers and it will generate One file "input.pdf" this file will contain the display of our digits in the input file.
 
  Another pdf file named "output.pdf" will be generated . This file will contain the digital image of the input passed and the predicted number by the model as label.

Another file called "h2o_prediction.csv" will be generated which will contain the preiction labels of our h2o deeplearning model.